# Include from /etc/nginx/nginx.conf inside http { }:
#   include /etc/nginx/conf.d/websocket-map.conf;
#
# Required for WebSocket (/ws) when proxying HTTPS → Docker.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
